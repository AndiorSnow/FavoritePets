# source 'https://github.com/CocoaPods/Specs.git'

# Uncomment the next line to define a global platform for your project
# platform :ios, '17.2'

#project 'FavoritePets'
workspace 'FavoritePets'
use_frameworks! 

target 'FavoritePets' do
  # Comment the next line if you don't want to use dynamic frameworks
  # Pods for FavoritePets
  
  project 'FavoritePets'
  use_frameworks!
  pod 'Kingfisher', '~> 7.0'
  pod 'SnapKit', '~> 5.6'

  target 'FavoritePetsTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'FavoritePetsUITests' do
    # Pods for testing
  end

end
